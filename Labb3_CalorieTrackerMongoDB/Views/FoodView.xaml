<UserControl x:Class="Labb3_CalorieTrackerMongoDB.Views.FoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Labb3_CalorieTrackerMongoDB.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F9F4"
             d:DataContext="{d:DesignInstance Type=viewmodels:FoodViewModel, IsDesignTimeCreatable=False}">

    <Border Padding="20" CornerRadius="12" Background="White" BorderBrush="#C1E1C1" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>


            <DataGrid x:Name="FoodListGrid"
                      ItemsSource="{Binding Foods}"
                      SelectedItem="{Binding SelectedFood}"
                      SelectionMode="Single"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Grid.Row="0"
                      Margin="0 0 0 10"
                      RowBackground="#F1F8F1"
                      AlternatingRowBackground="White"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      FontSize="14"
                      FontWeight="SemiBold">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Protein" Binding="{Binding Protein}" Width="80"/>
                    <DataGridTextColumn Header="Carbs" Binding="{Binding Carbs}" Width="80"/>
                    <DataGridTextColumn Header="Fat" Binding="{Binding Fat}" Width="80"/>
                    <DataGridTextColumn Header="Calories" Binding="{Binding Calories}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>


            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Add Food" Width="100" Height="32" Margin="5"
                        Command="{Binding AddFoodCommand}" 
                        Background="#A5D6A7" Foreground="#2E7D32" FontWeight="SemiBold" 
                        BorderBrush="#81C784" BorderThickness="1"/>
                <Button Content="Update Food" Width="100" Height="32" Margin="5"
                        Command="{Binding  UpdateFoodCommand}" 
                        CommandParameter="{Binding SelectedFood}"
                        Background="#81C784" Foreground="White" FontWeight="Bold"
                        BorderBrush="#66BB6A" BorderThickness="1"/>
                <Button Content="Delete Food" Width="100" Height="32" Margin="5"
                        Command="{Binding DeleteCommand}" 
                         CommandParameter="{Binding SelectedFood}"
                        Background="#EF9A9A" Foreground="White" FontWeight="Bold"
                        BorderBrush="#E57373" BorderThickness="1"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

