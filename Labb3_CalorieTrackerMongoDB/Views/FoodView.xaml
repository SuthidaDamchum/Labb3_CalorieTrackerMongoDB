<UserControl x:Class="Labb3_CalorieTrackerMongoDB.Views.FoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_CalorieTrackerMongoDB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <DataGrid x:Name="FoodListGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Grid.Row="0"
                  Margin="0 0 0 10"
                  SelectionMode="Single" >
            <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding }" Width="100"/>
            <DataGridTextColumn Header="Protein" Binding="{Binding Protein}" Width="80"/>
            <DataGridTextColumn Header="Carbs" Binding="{Binding Carbs}" Width="80"/>
            <DataGridTextColumn Header="Fat" Binding="{Binding Fat}" Width="80"/>
            <DataGridTextColumn Header="Calories" Binding="{Binding Calories}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add Food" Width="100" Margin="5" Click="AddFood_Click"/>
            <Button Content="Update Food" Width="100" Margin="5" Click="UpdateFood_Click"/>
            <Button Content="Delete Food" Width="100" Margin="5" Click="DeleteFood_Click"/>
        </StackPanel>
    </Grid>
</UserControl>