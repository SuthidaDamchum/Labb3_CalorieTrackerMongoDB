<UserControl x:Class="Labb3_CalorieTrackerMongoDB.Views.WeeklySummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Labb3_CalorieTrackerMongoDB.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=viewmodels:WeeklySummaryViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>

        <SolidColorBrush x:Key="MatchaPrimary" Color="#7A8450"/>
        <SolidColorBrush x:Key="MatchaPrimaryDark" Color="#5F6B3A"/>
        <SolidColorBrush x:Key="VanillaBackground" Color="#F8EFDA"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushVanilla" Color="#D8D1B8"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#3F4A2C"/>
        <SolidColorBrush x:Key="TextMuted" Color="#8A8F75"/>

      
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushVanilla}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.12"/>
                </Setter.Value>
            </Setter>
        </Style>

     
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource MatchaPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="8,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource MatchaPrimaryDark}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#C9CFB2"/>
                    <Setter Property="Foreground" Value="#F5F5F5"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style TargetType="DataGrid">
            <Setter Property="RowBackground" Value="#FAF7EE"/>
            <Setter Property="AlternatingRowBackground" Value="#F3EFDF"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E1E6C9"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="MinHeight" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasLog}" Value="False">
                    <Setter Property="Opacity" Value="0.45"/>
                </DataTrigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D6E1B4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="FocusCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="#FFF9E5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource MatchaPrimary}"/>
        </Style>
        <Style x:Key="FocusHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5E8B7"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource MatchaPrimaryDark}"/>
        </Style>
        <Style x:Key="CenterHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource VanillaBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border Margin="8" Style="{StaticResource CardStyle}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>


                <StackPanel Grid.Row="0">
                    <TextBlock Text="Weekly Summary"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource MatchaPrimary}"/>
                    <TextBlock Text="{Binding WeekRange}"
                               Margin="0,4,0,0"
                               FontSize="12"
                               Foreground="{StaticResource TextMuted}"/>
                </StackPanel>


                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="0,10,0,0">
                    <Button Content="◀ Prev"
                            Style="{StaticResource NavButtonStyle}"
                            Height="28"
                            MinWidth="70"
                            Command="{Binding PrevWeekCommand}"/>

                    <Button Content="This week"
                            Margin="8,0,8,0"
                            Style="{StaticResource NavButtonStyle}"
                            Height="28"
                            MinWidth="70"
                            Command="{Binding ThisWeekCommand}"/>

                    <Button Content="Next ▶"
                            Style="{StaticResource NavButtonStyle}"
                            Height="28"
                            MinWidth="70"
                            Command="{Binding NextWeekCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="18,0,18,18" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

    
                <Border Grid.Row="0"
                        Margin="0,0,0,12"
                        Padding="14"
                        CornerRadius="14"
                        Background="#FFFDF7"
                        BorderBrush="{StaticResource BorderBrushVanilla}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="This week:"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Margin="8,0,0,0"
                                       Text="{Binding WeekCaloriesSummaryText}"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="{Binding DaysLogged, StringFormat=Logged: {0}/7}"
                                       Foreground="{StaticResource TextMuted}"
                                       FontWeight="SemiBold"/>
                            <TextBlock Margin="14,0,0,0"
                                       Text="{Binding AvgCalories, StringFormat=Avg kcal: {0:F0}}"
                                       Foreground="{StaticResource TextMuted}"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding WeekItems}"
                          AutoGenerateColumns="False" SelectionChanged="DataGrid_SelectionChanged">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Day"
                                            Width="170"
                                            Binding="{Binding Date, StringFormat='{}{0:ddd, MMM d}'}"/>

                        <DataGridTextColumn Header="Goal" Width="80" Binding="{Binding GoalCalories}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>
                        <DataGridTextColumn Header="Actual" Width="80" Binding="{Binding ActualCalories}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>
                        <DataGridTextColumn Header="Diff" Width="80" Binding="{Binding CaloriesDiffText}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>

                        <DataGridTextColumn Header="Protein (A/G)" Width="120" Binding="{Binding ProteinText}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>
                        <DataGridTextColumn Header="Carbs (A/G)" Width="120" Binding="{Binding CarbsText}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>
                        <DataGridTextColumn Header="Fat (A/G)" Width="120" Binding="{Binding FatText}" 
                    CellStyle="{StaticResource FocusCellStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>