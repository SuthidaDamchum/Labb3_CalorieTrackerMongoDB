<UserControl x:Class="Labb3_CalorieTrackerMongoDB.Views.WeeklySummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_CalorieTrackerMongoDB.Views"
             xmlns:viewmodels="clr-namespace:Labb3_CalorieTrackerMongoDB.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
            d:DataContext="{d:DesignInstance Type=viewmodels:WeeklySummaryViewModel, IsDesignTimeCreatable=False}">


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


   
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Weekly Statistics" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding WeekRange}" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            <TextBlock Text="Prot: Protein, Carb: Carbohydrates, Fat: Fat, Kcal: Calories" FontSize="12" HorizontalAlignment="Center"/>
            <TextBlock Text="Result: ✅ = Goal met or under, ⚠️ = Exceeded goal" FontSize="12" FontStyle="Italic" HorizontalAlignment="Center"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding WeeklyLogs}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Goal Kcal" Binding="{Binding GoalCalories}" Width="80"/>
                <DataGridTextColumn Header="Actual Kcal" Binding="{Binding ActualCalories}" Width="100"/>
                <DataGridTextColumn Header="Prot" Binding="{Binding Protein}" Width="80"/>
                <DataGridTextColumn Header="Carb" Binding="{Binding Carbs}" Width="80"/>
                <DataGridTextColumn Header="Fat" Binding="{Binding Fat}" Width="80"/>
                <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>