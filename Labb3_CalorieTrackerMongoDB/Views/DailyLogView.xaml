<UserControl x:Class="Labb3_CalorieTrackerMongoDB.Views.DailyLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Labb3_CalorieTrackerMongoDB.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DailyLogViewModel, IsDesignTimeCreatable=True}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- DatePicker -->
            <RowDefinition Height="Auto"/>
            <!-- Goals -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Totals -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="{Binding TodayDate, StringFormat='Date: {0:d}'}"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- DatePicker (no button) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Select Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Width="140"/>
        </StackPanel>

        <!-- Goals -->
        <Border Grid.Row="2" Background="#EEE" Padding="10" CornerRadius="8" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Daily Goals" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                <WrapPanel>
                    <TextBlock Text="Calories:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding GoalCalories}" Margin="5,0,15,0"/>
                    <TextBlock Text="Protein:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding GoalProtein}" Margin="5,0,15,0"/>
                    <TextBlock Text="Carbs:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding GoalCarbs}" Margin="5,0,15,0"/>
                    <TextBlock Text="Fat:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding GoalFat}"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding TodaysItems}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Food" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="80"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Protein" Binding="{Binding Protein}" Width="80"/>
                <DataGridTextColumn Header="Carbs" Binding="{Binding Carbs}" Width="80"/>
                <DataGridTextColumn Header="Fat" Binding="{Binding Fat}" Width="80"/>
                <DataGridTextColumn Header="Calories" Binding="{Binding Calories}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals -->
        <Border Grid.Row="4" Background="#F5F5F5" Padding="10" CornerRadius="8" Margin="0,0,0,10">
            <WrapPanel HorizontalAlignment="Right">
                <TextBlock Text="Total Calories:" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalCalories}" Margin="5,0,15,0"/>
                <TextBlock Text="Protein:" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalProtein}" Margin="5,0,15,0"/>
                <TextBlock Text="Carbs:" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalCarbs}" Margin="5,0,15,0"/>
                <TextBlock Text="Fat:" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalFat}"/>
            </WrapPanel>
        </Border>

        <!-- Delete button -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Delete"
                    Command="{Binding DeleteItemCommand}"
                    CommandParameter="{Binding SelectedItem}"
                    Width="120"/>
        </StackPanel>
    </Grid>
</UserControl>